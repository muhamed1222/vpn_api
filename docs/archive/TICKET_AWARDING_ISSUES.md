# Возможные проблемы с автоматическим начислением билетов

## Основные причины, почему билеты могут не начисляться:

### 1. ✅ Заказ уже обработан (идемпотентность)
**Проблема:** В `processPayment()` (строка 61) есть проверка:
```typescript
if (order.status === OrderStatus.PAID || order.status === OrderStatus.COMPLETED) {
    return; // Пропускает обработку
}
```
**Решение:** Если заказ уже COMPLETED, нужно вызвать `activateOrder()` напрямую, чтобы проверить билеты.

### 2. ❌ Заказ обрабатывается через API webhook, но его нет в API базе
**Проблема:** Если заказ обработан напрямую ботом, он может отсутствовать в API базе (`ordersRepo.getOrder()` вернет `null`).
**Следствие:** `awardTicketsForPayment` из API не вызовется.
**Решение:** Бот должен начислить билеты через `ContestService.awardSelfPurchaseTicket`.

### 3. ⚠️ Проверка активного конкурса
**Проблема:** В `contestService.ts` функция `getActiveContest()` может вернуть `null` если:
- Нет активного конкурса (`is_active = 0`)
- Несколько конкурсов и логика выбора неправильная
**Решение:** Проверить `SELECT * FROM contests WHERE is_active = 1`

### 4. ⏰ Проверка периода конкурса
**Проблема:** В `awardSelfTickets()` проверяется `orderCreatedAt`:
```typescript
if (orderCreatedAt < contestStartsAt) {
    return false; // Заказ создан до начала конкурса
}
if (orderCreatedAt > contestEndsAt) {
    return false; // Заказ создан после окончания конкурса
}
```
**Важно:** `orderCreatedAt` должен быть в **секундах** (Unix timestamp), а не миллисекундах!
**Проблема:** В `awardSelfPurchaseTicket` вызывается `Math.floor(orderCreatedAt / 1000)`, но если `orderCreatedAt` уже в секундах - будет ошибка.

### 5. 🎫 Билеты уже начислены (идемпотентность)
**Проблема:** Проверка существующих билетов:
```typescript
const existing = database.prepare(`
    SELECT id FROM ticket_ledger 
    WHERE contest_id = ? AND referrer_id = ? AND referred_id = ? 
      AND order_id = ? AND reason = 'SELF_PURCHASE'
`).get(contestId, userId, userId, orderId);
if (existing) {
    return false; // Уже начислены
}
```
**Решение:** Это нормально - функция идемпотентна.

### 6. ⚠️ Ошибки выполнения (try-catch игнорирует ошибки)
**Проблема:** В `awardTicketsForPayment` (API) и `awardSelfTickets` (бот) есть try-catch, которые могут скрыть ошибки:
```typescript
try {
    // ... начисление билетов
} catch (error) {
    console.error(`[awardTicketsForPayment] Error:`, error);
    return false;
}
```
**Следствие:** Ошибки логируются, но билеты не начисляются.

### 7. 📋 Таблица `ticket_ledger` отсутствует
**Проблема:** В `awardTicketsForPayment` проверяется наличие таблицы:
```typescript
if (!ticketLedgerExists) {
    console.warn('[awardTicketsForPayment] ticket_ledger table not found');
    return false;
}
```
**Решение:** Проверить наличие таблицы в базе бота.

### 8. 🔍 Неправильный `plan_id`
**Проблема:** Если `plan_id` не найден или `plan_7` (пробный тариф):
```typescript
if (planId === 'plan_7') {
    console.log(`[ContestService] Plan plan_7 does not award tickets`);
    return false;
}
```
**Решение:** Проверить, что `plan_id` правильный и не `plan_7`.

### 9. 🕐 Проблема с часовыми поясами
**Проблема:** Проверка периода конкурса может некорректно работать из-за:
- Разницы между UTC и локальным временем
- Неправильного формата `starts_at` / `ends_at` в базе
- Конвертации timestamp (миллисекунды vs секунды)
**Решение:** Убедиться, что все даты в UTC и правильно конвертируются.

### 10. 🔄 Заказ обрабатывается не через централизованные функции
**Проблема:** Если заказ устанавливается в COMPLETED напрямую (например, через SQL):
- `activateOrder()` не вызывается
- Билеты не начисляются
**Решение:** Всегда использовать `processPayment()` или `activateOrder()`.

## Как диагностировать проблему:

1. **Проверить логи:**
   ```bash
   journalctl -u vpn-bot.service | grep -iE 'awardSelfPurchaseTicket|Contest.*not active|Order.*created before'
   journalctl -u outlivion-api.service | grep -iE 'awardTicketsForPayment|outside contest period'
   ```

2. **Проверить активный конкурс:**
   ```sql
   SELECT id, title, is_active, starts_at, ends_at FROM contests WHERE is_active = 1;
   ```

3. **Проверить наличие билетов:**
   ```sql
   SELECT * FROM ticket_ledger WHERE order_id = 'ORDER_ID';
   ```

4. **Проверить статус заказа:**
   ```sql
   SELECT id, status, created_at FROM orders WHERE id = 'ORDER_ID';
   ```

5. **Проверить наличие таблиц:**
   ```sql
   SELECT name FROM sqlite_master WHERE type='table' AND name IN ('ticket_ledger', 'ref_events');
   ```

## Рекомендации:

1. ✅ Добавить больше логирования в функции начисления билетов
2. ✅ Проверить, что `orderCreatedAt` передается в правильном формате (миллисекунды для API, секунды для бота после конвертации)
3. ✅ Убедиться, что все заказы обрабатываются через `processPayment()` или `activateOrder()`
4. ✅ Проверить, что конкурс активен и заказ попадает в период конкурса
