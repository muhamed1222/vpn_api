# 🚨 Критический анализ архитектуры Outlivion VPN

**Дата анализа:** 12.01.2026  
**Статус:** ⚠️ Требует немедленного вмешательства перед релизом

Этот документ описывает критические уязвимости и архитектурные ошибки, обнаруженные в ходе анализа экосистемы (iOS App, API, Telegram Bot, Web App). Игнорирование этих проблем несет высокие риски для бизнеса и стабильности продукта.

---

## 1. 💣 "Рассинхронизация" данных (Split Brain)
**Приоритет:** 🔥 КРИТИЧЕСКИЙ (Блокирует релиз)

### Проблема
В проекте используются **две независимые базы данных**:
1. `vpn_bot` использует свою локальную `database.sqlite`.
2. `vpn_api` использует свою локальную `db.sqlite`.

### Последствия
*   **Потеря покупок:** Пользователь оплачивает подписку в боте -> запись идет в БД бота. Пользователь заходит в приложение -> API ищет запись в своей БД -> не находит -> **подписки нет**.
*   **Невозможность поддержки:** Поддержка не видит реального состояния пользователя, так как данные размазаны по разным файлам.

### 🛠 Решение
**Единый источник правды.**
1.  **Миграция на PostgreSQL:** Развернуть один экземпляр PostgreSQL (в Docker).
2.  **Подключение:** Настроить `vpn_api` и `vpn_bot` на подключение к этой одной базе.
3.  **Альтернатива (если оставляем SQLite):** API становится единственным владельцем БД. Бот перестает писать в базу напрямую и начинает работать через HTTP-запросы к API (например, `POST /internal/orders/create`).

---

## 2. 📱 iOS: Фейковая статистика и Риск реджекта
**Приоритет:** 🔴 ВЫСОКИЙ (Риск удаления из Store)

### Проблема
В `vpn2` (iOS) реализована **симуляция** статистики трафика. Графики рисуются "из головы", а не на основе реальных данных туннеля.

### Последствия
*   **App Store Review:** Apple тестирует приложения. Если они заметят активность на графике при отсутствии реальной передачи данных (или наоборот), приложение будет отклонено за "Misleading Functionality".
*   **Утрата доверия:** Пользователи видят обман -> пользователи уходят.

### 🛠 Решение
Реализовать сбор реальной статистики через **App Groups**:
1.  В `PacketTunnelProvider` (Network Extension) считать байты (`readBytes`, `writeBytes`).
2.  Записывать эти данные в Shared Container (файл или `UserDefaults` в App Group).
3.  В основном приложении читать эти данные и обновлять UI.

---

## 3. 💣 iOS: "Бомба" с SSL Certificate Pinning
**Приоритет:** 🔴 ВЫСОКИЙ (Риск отказа в обслуживании)

### Проблема
В коде iOS приложения жестко зашит хэш (fingerprint) текущего SSL-сертификата сервера.

### Последствия
SSL-сертификаты имеют срок действия. Как только сертификат на сервере обновится (автоматически через 3 месяца или вручную):
*   Хэш изменится.
*   Приложение **мгновенно перестанет соединяться с сервером**.
*   Пользователи не смогут даже получить обновление конфига, так как соединение будет блокироваться самим приложением. Приложение превратится в "кирпич".

### 🛠 Решение
1.  **Public Key Pinning:** Пинить не сам сертификат, а его публичный ключ (он не меняется при перевыпуске сертификата).
2.  **Remote Config / Fallback:** Реализовать механизм получения экстренного конфига через независимый канал (например, JSON файл на GitHub или Firebase Remote Config), который скажет приложению: "Отключи пиннинг, сертификат сменился".

---

## 4. 💸 Финансы: SQLite и Отсутствие тестов
**Приоритет:** 🟠 СРЕДНИЙ (Финансовые риски)

### Проблема
1.  **SQLite в проде:** SQLite блокирует файл при записи. При массовой оплате (например, после рассылки) запросы начнут падать с ошибкой `SQLITE_BUSY`.
2.  **Нет тестов:** В `src/routes/v1/payments.ts` нет автотестов. Любое изменение кода может сломать прием денег.

### 🛠 Решение
1.  Переход на PostgreSQL (решает проблему блокировок).
2.  Написать интеграционные тесты для Webhook-ендпоинтов (эмуляция запросов от YooKassa).

---

## 5. 🕸️ Web: Безопасность Telegram Mini App
**Приоритет:** 🔴 ВЫСОКИЙ (Уязвимость)

### Проблема
В спецификации отмечено: *"Безопасность WebApp - Нужно усиление валидации"*.

### Последствия
Если бэкенд не проверяет криптографическую подпись `initData` от Telegram при КАЖДОМ запросе:
*   Злоумышленник может подделать `user_id` в заголовках/теке.
*   Получить доступ к чужим подпискам или управлению аккаунтом.

### 🛠 Решение
Внедрить Middleware на бэкенде, который:
1.  Парсит строку `initData`.
2.  Проверяет HMAC-SHA256 подпись, используя токен бота.
3.  Отклоняет любые запросы без валидной подписи.

---

## 📋 План действий (Roadmap)

1.  **Неделя 1 (Фундамент):**
    *   [ ] Поднять PostgreSQL.
    *   [ ] Мигрировать схемы данных из бота и API в Postgres.
    *   [ ] Переписать DAL (Data Access Layer) в боте и API на работу с Postgres.

2.  **Неделя 2 (iOS Fixes):**
    *   [ ] Убрать симуляцию статистики, реализовать чтение из App Group.
    *   [ ] Исправить стратегию SSL Pinning (реализовать Fallback механизм).

3.  **Неделя 3 (Стабилизация):**
    *   [ ] Написать тесты на платежи.
    *   [ ] Реализовать строгую валидацию `initData` в API.
    *   [ ] Настроить CI/CD для автоматического прогона тестов.
